const {describe, it} = require("mocha");
const {should} = require("chai").should();

const DB = require("../dist/database/connectDB");

describe("Test connection to DataBase!", () => {
  it("Connect to mongodb://localhost:27017/Forum", async () => {});
});
